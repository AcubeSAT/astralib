{% extends '@SonataAdmin/standard_layout.html.twig' %}

{% block title %}
    {{ 'PDF files' }}
{% endblock %}

{% block navbar_title %}
    {{ 'PDF file debugging' }}
{% endblock %}

{% block sonata_admin_content %}
    <div class="row">
        <div class="col-md-6">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Usage instructions</h3>
                </div>
                <div class="box-body">
                    <p>
                        This page allows you to upload a PDF file and see how the parser processes it. After uploading a
                        file here,
                        you will be able to review all the extracted details, including metadata, versions, and the
                        thumbnail.
                    </p>

                    <p>
                        This tool will be useful when examining different PDF files or PHP modules, to see if the
                        internal
                        details are parsed correctly.
                    </p>

                    <p>
                        All files uploaded here will <em>not</em> be stored in the database or the file system. You can
                        upload
                        any content you want here, and it will be deleted after use.
                    </p>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="box box-info">
                <div class="box-header">
                    <h3 class="box-title">Upload form</h3>
                </div>
                <div class="box-body">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>

    {% if source|default(null) %}
        {{ dump(source) }}
    {% endif %}
{% endblock %}
